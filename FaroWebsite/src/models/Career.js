export class Career {
  constructor(params) {
    this.idCareer = params.idCareer;
    this.careerName = params.careerName;
    this.careerDescription = params.careerDescription;
    this.degree = params.degree;
    this.duration = params.duration;
    this.keywords = params.keywords;
  }
}
